# Copy to `.env` in the repo root (`.env` is gitignored). Never commit real values.

# Optional: Tauri updater / signing (see https://v2.tauri.app/distribute/)
# TAURI_PRIVATE_KEY=
# TAURI_PRIVATE_KEY_PASSWORD=

# Application/API configuration
# API_KEY=

# Desktop admin login (compiled into the Rust binary — set before `tauri build` / release CI)
# IMPORT_MANAGER_ADMIN_USERNAME=Jana
# IMPORT_MANAGER_ADMIN_PASSWORD_HASH=<bcrypt hash of admin password>

# Playwright E2E (optional; default password used when unset — see tests)
# E2E_PASSWORD=
